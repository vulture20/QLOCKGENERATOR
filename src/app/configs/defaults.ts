import { IFont, IFontAlarm, IVariant } from './../core/core.module';
import { FONTS, ALARM_SYMBOLS } from './fonts';
import { VARIANTS } from './variants';

export const DEFAULT_FONT: IFont = FONTS[3];
export const DEFAULT_ALARM_SYMBOL_NONE: IFontAlarm = ALARM_SYMBOLS[0];
export const DEFAULT_ALARM_SYMBOL: IFontAlarm = ALARM_SYMBOLS[3];

export const DEFAULT_VARIANT: IVariant = VARIANTS[0];

